
// localStorage keys are removed as we are moving towards a backend-based approach.
// export const LOCAL_STORAGE_KEY = 'travelJournalEntries';
// export const COMPANIONS_LOCAL_STORAGE_KEY = 'travelJournalCompanions';

export const GEMINI_TEXT_MODEL = 'gemini-2.5-flash-preview-04-17';

// Example roles (could be expanded)
export const USER_ROLES = {
  ADMIN: 'admin',
  USER: 'user',
} as const;
